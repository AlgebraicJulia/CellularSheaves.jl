# In cache/, track ONLY the trained policies (*.jld2). Every rendered figure lives in a cache/
# subdirectory (viz/, viz_tv/, viz_multiagent/, ...) and is regenerable via viz/*.jl or
# refresh_viz.sh — keep it all out of git history.
cache/*/

# Julia environment resolution (regenerated by Pkg.instantiate)
Manifest.toml
