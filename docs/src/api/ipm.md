# IPM

An interior-point method solver for cone programs (semidefinite, second-order, and
positive cones), used internally to solve the KKT systems arising from constrained
sheaf-based control problems.

```@autodocs
Modules = [CellularSheaves.IPM]
```
